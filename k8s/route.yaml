apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: quickbank
  labels:
    app: quickbank
spec:
  to:
    kind: Service
    name: quickbank
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
